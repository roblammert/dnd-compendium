# D&D Compendium v0.33.2 Patch

Apply this patch to a **v0.33.1** project root:

```bash
./install_patch.sh /path/to/dnd-compendium
```

The installer creates no backups, overwrites only the files included in this patch, appends the v0.33.2 release notes once, and prints verification and Git commit commands.
