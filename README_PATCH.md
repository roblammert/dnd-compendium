# D&D Compendium v0.32.8 Patch

Apply this patch to a v0.32.7 project root:

```bash
./install_patch.sh /path/to/dnd-compendium
```

The installer creates no backups, overwrites only the files included in the patch, appends the v0.32.8 release notes once, and prints verification and Git commit commands.
