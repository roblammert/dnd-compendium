# v0.19.0 Upgrade Patch

Run `./install_patch.sh /path/to/dnd-compendium`. The installer creates timestamped backups before replacing existing files. Then run `pip install -e ' .[dev]'` without the space (see release response), configure authentication variables in `.env`, and restart the service.
